# Pinecone and OpenAI
# Pin Pinecone to v8.x — the integrated-inference search API used in
# main.py (index.search(namespace=..., query={...}, rerank={...})) is
# only stable in v8. Newer majors have renamed/removed the `query` kwarg.
pinecone>=8.0.0,<9.0.0
openai>=1.0.0

# PDF processing
pdf2image>=1.16.0
Pillow>=10.0.0

# Environment variables
python-dotenv>=1.0.0

# Web frontend
streamlit>=1.35.0
streamlit-cookies-controller>=0.0.4
cryptography>=42.0.0
